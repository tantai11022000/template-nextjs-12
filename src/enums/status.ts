export enum USER_STATUS {
    ACTIVE = 1,
    INACTIVE = 2,
}

export enum TARGET_BIDDING_STATUS {
    ACTIVE = 1,
    INACTIVE = 2,
}

export enum CAMPAIGN_STATUS {
    ENABLE = 1,
    PAUSED = 2,
    ARCHIVED = 3,
    ENDED = 4,
    OTHER = 5
}

export enum SCHEDULE_STATUS {
    UPCOMING = 0,
    SUCCESSFULLY_EXECUTED = 1,
    FAILED_EXECUTED = 2,
    IN_QUEUE = 3,
    PROCESSING = 4,
}

